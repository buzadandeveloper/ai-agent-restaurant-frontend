pre-commit:
  parallel: true
  commands:
    biome:
      run: pnpm lint {staged_files}
      glob: "src/**/*.{ts,tsx,js,json}"
    tsc:
      run: pnpm typecheck
      glob: "src/**/*.{ts,tsx}"

commit-msg:
  parallel: true
  commands:
    commitlint:
      run: pnpm commitlint --edit $1